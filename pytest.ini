[pytest]
addopts = -q -m "not acceptance"
testpaths =
    tests
markers =
    providers: External provider client tests
    models: Financial domain models tests
    acceptance: Live provider acceptance tests
